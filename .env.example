# Vercel Deploy Hook URL (get from Vercel project settings → Git → Deploy Hooks)
VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/prj_xxx/yyy

# Optional: GitHub integration for auto-commit/push
GITHUB_TOKEN=ghp_xxx
GITHUB_REPO_OWNER=your-org
GITHUB_REPO_NAME=your-repo

# Optional: OpenAI / AI provider key (for future AI-powered steps)
OPENAI_API_KEY=sk-xxx

# App base URL (used for internal API calls in workflows)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── AI / LLM Integration ──────────────────────────────────────────────────────

# Anthropic API key — required for POST /api/ai (Claude integration)
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default Claude model
# Default: claude-3-5-sonnet-20241022
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# ── Auth & Security ───────────────────────────────────────────────────────────

# Secret key used to sign and verify JWTs (HS256)
# Generate a strong random value: openssl rand -base64 32
AUTH_SECRET=change-me-in-production

# Secret required to issue admin-role tokens via POST /api/auth/token
ADMIN_SECRET=change-me-in-production

# JWT expiry duration (e.g. "8h", "1d", "30m")
JWT_EXPIRY=8h

# ── Rate Limiting & Quotas ────────────────────────────────────────────────────

# These are compile-time defaults in lib/rate-limiter.ts; override here if needed
# RATE_LIMIT_REQUESTS_PER_MINUTE=60
# RATE_LIMIT_EXECUTIONS_PER_HOUR=100
# RATE_LIMIT_TOKENS_PER_DAY=1000000

# ── Audit Logging ─────────────────────────────────────────────────────────────

# Directory where audit.jsonl and history.jsonl are written (default: <cwd>/logs)
# AUDIT_LOG_DIR=/var/log/coded
